"""Rails sub-package — reserved for future five-rail implementations.

Each rail (Input, Retrieval, Dialog, Execution, Output) may be extracted
into its own module here as the pipeline grows in complexity.  For now all
rail logic lives directly in :mod:`kitty.pipeline.evaluator`.
"""

__all__: list[str] = []
